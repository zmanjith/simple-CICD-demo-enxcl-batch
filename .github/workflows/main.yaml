# name: CI Pipeline

# on:
#   push:
#     branches:
#       - main

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Java
#         uses: actions/setup-java@v4
#         with:
#           distribution: temurin
#           java-version: 17

#       - name: Run Maven Test
#         run: mvn test

#       - name: Docker Login
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}

#       - name: Build Image
#         run: docker build -t simple-html-app .

#       - name: Tag Image
#         run: docker tag simple-html-app zmanjith/simple-html-app:v3

#       - name: Push Image
#         run: docker push zmanjith/simple-html-app:v3

#       - name: Deploy to EC2
#         uses: appleboy/ssh-action@master
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ubuntu
#           key: ${{ secrets.EC2_SSH_KEY }}
#           script: |

#             docker pull zmanjith/simple-html-app:v3

#             docker stop myapp || true

#             docker rm myapp || true

#             docker run -d \
#             --name myapp \
#             -p 80:80 \
#             zmanjith/simple-html-app:v3
